# Playwright + Pytest UI Experiments  
### SauceDemo

---

## 📌 Описание

Репозиторий используется как рабочая песочница для экспериментов  
с UI-автотестами на связке **Python + Pytest + Playwright**  
на примере публичного тестового приложения **SauceDemo**.

Цель проекта — практическая отработка подходов,  
а не демонстрация финальной или «идеальной» архитектуры.

---

## 🎯 Назначение репозитория

Проект создавался для:

- проверки различных UI-паттернов
- экспериментов с локаторами и ожиданиями
- отработки end-to-end сценариев
- итеративного развития автотестов по мере появления новых задач

Архитектура и структура намеренно остаются гибкими  
и могут изменяться со временем.

---

## 🧩 Текущий функционал

На данный момент реализовано:

- базовые UI smoke-тесты
- сценарий логина
- конфигурация Pytest
- использование Playwright `expect` с автоожиданиями
- работа с `data-*` атрибутами как основными локаторами

Проект не перегружен абстракциями и дополнительными слоями,  
чтобы ускорить эксперименты и итерации.

---

## 🛠 Используемый стек

- Python
- Pytest
- Playwright (Sync API)

---

## 📂 Структура проекта

```text
.
├── tests/
│   └── ui/
│       ├── test_login.py
│       └── test_smoke.py
├── pytest.ini
├── .gitignore
└── README.md

---

## ▶️ Запуск тестов

pip install -r requirements.txt
playwright install

Запуск всех тестов:

pytest


